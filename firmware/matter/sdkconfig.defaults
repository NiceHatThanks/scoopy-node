CONFIG_ESPTOOLPY_FLASHSIZE_4MB=y

# BLE commissioning rendezvous
CONFIG_BT_ENABLED=y
CONFIG_BT_NIMBLE_ENABLED=y
CONFIG_BT_NIMBLE_ENABLE_CONN_REATTEMPT=n

# Matter/IP networking
CONFIG_LWIP_IPV6_AUTOCONFIG=y
CONFIG_LWIP_HOOK_IP6_ROUTE_DEFAULT=y
CONFIG_LWIP_HOOK_ND6_GET_GW_DEFAULT=y
CONFIG_LWIP_IPV6_NUM_ADDRESSES=6
CONFIG_ENABLE_WIFI_AP=n
CONFIG_ESP_WIFI_SOFTAP_SUPPORT=n

# 4 MB dual-slot layout, OTA requestor intentionally disabled for now
CONFIG_PARTITION_TABLE_CUSTOM=y
CONFIG_PARTITION_TABLE_FILENAME="partitions.csv"
CONFIG_PARTITION_TABLE_OFFSET=0xC000
CONFIG_ENABLE_OTA_REQUESTOR=n

# Development diagnostics
CONFIG_ENABLE_CHIP_SHELL=y

# Matter storage/crypto
CONFIG_ESP_MATTER_NVS_USE_COMPACT_ATTR_STORAGE=y
CONFIG_MBEDTLS_HKDF_C=y

# ESP-Matter's managed component includes bridge support, whose default
# aggregator endpoint consumes one dynamic endpoint. Keep two available so
# a non-bridge Scoopy build still compiles the bridge component safely.
CONFIG_ESP_MATTER_MAX_DYNAMIC_ENDPOINT_COUNT=2

# Keep multicast/groupcast surface minimal until required
CONFIG_SUPPORT_GROUPCAST_CLUSTER=n
