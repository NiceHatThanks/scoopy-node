# ESPHome firmware

ESPHome configuration for Scoopy Node will live here.

The initial firmware will focus on the two buttons and status LEDs, followed by optional mmWave radar and I²C expansion support.

Provisioning, default entities, LED behaviours, and update instructions will be documented here as the firmware is finalised.
